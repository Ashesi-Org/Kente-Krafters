INSERT INTO orders (user_id, total_price) VALUES (user_id_value, total_price_value);
Add Items to Order:

sql
Copy code
INSERT INTO order_items (order_id, product_id, quantity, price) VALUES (order_id_value, product_id_value, quantity_value, price_value);
Apply Promotions to Order:

sql
Copy code
INSERT INTO order_promotions (order_id, promo_id) VALUES (order_id_value, promo_id_value);
Make Payment:

sql
Copy code
INSERT INTO payments (order_id, payment_method, amount) VALUES (order_id_value, 'payment_method_value', amount_value);
Add Shipping Information:

sql
Copy code
INSERT INTO shipping (order_id, tracking_number, address) VALUES (order_id_value, 'tracking_number_value', 'address_value');
Adding to Cart:
Add Product to Shopping Cart:
sql
Copy code
INSERT INTO shopping_cart (user_id, product_id, quantity) VALUES (user_id_value, product_id_value, quantity_value);
Removing from Cart:
Remove Product from Shopping Cart:
sql
Copy code
DELETE FROM shopping_cart WHERE user_id = user_id_value AND product_id = product_id_value;
Making Payment:
Make Payment for an Order:
sql
Copy code
INSERT INTO payments (order_id, payment_method, amount) VALUES (order_id_value, 'payment_method_value', amount_value);
Browsing Products:
Get All Products:

sql
Copy code
SELECT * FROM products;
Filter Products by Category:

sql
Copy code
SELECT * FROM products WHERE category_id = category_id_value;
Get Product Details:

sql
Copy code
SELECT * FROM products WHERE product_id = product_id_value;
Get Products in Stock:

sql
Copy code
SELECT * FROM products WHERE stock_quantity > 0;
Get Product Reviews:

sql
Copy code
SELECT * FROM reviews WHERE product_id = product_id_value;
Search Products by Name:

sql
Copy code
SELECT * FROM products WHERE name ILIKE '%search_query%'